# 04中， 为什么create的时候需要profile 账户的签名，但是update的时候不需要？

profile create账户的时候，是本地通过keypair派生出的账户和地址（有私钥）, profile在客户端本地生成后，需要在链上把这个地址占上.  
验证的时候，需要验证这个地址是不是这个profile私钥生成的，如果不验证，那么用户可以放任意一个地址，可能把别人的地址占了。    
但是，一但profile账户生成并在链上后，更新它不需要profile的私钥，验证变成了profile的所有者是不是用户。